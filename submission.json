{
    "config": ["config/data-params.json", "config/<name>.json"],  # paths of at least 2 config files
    "build": "run.py", # path of your build script
    "library": ["src"], # directory containing (your) library code
    "notebook": ["notebooks/1-joey-eda.ipynb"], # path to at least one notebook meant for reading by someone other than you.
    "targets": ["data_prep", "autophrase"] # at least two targets
}
